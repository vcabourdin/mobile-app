define([
  // Application.
  "app"
],

// Map dependencies from above array.
function(app) {

  // Create a new module.
  var Quiz = app.module();

  

  // Return the module for AMD compliance.
  return Quiz;

});
